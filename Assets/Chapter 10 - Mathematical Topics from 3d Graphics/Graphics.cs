using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// find a middle center points of frustum planes
// find fov and zoom value of perspective projection
// find clipped coordinates of some object.
// translate object from near clip bottom-left to far clip top-right
// mimic camera.screentoworld 
// mimic camera.screentoray funtions


public class Graphics : MonoBehaviour
{
    public Camera cam;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
